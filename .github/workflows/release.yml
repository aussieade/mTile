name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build release
        run: |
          xcodebuild \
            -project mTile.xcodeproj \
            -scheme mTile \
            -configuration Release \
            -destination "generic/platform=macOS" \
            -derivedDataPath build \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Verify app architectures
        run: |
          lipo -info build/Build/Products/Release/mTile.app/Contents/MacOS/mTile

      - name: Package DMG
        run: |
          rm -rf build/dmg-staging
          mkdir -p build/dmg-staging
          cp -r build/Build/Products/Release/mTile.app build/dmg-staging/
          ln -s /Applications build/dmg-staging/Applications
          hdiutil create \
            -volname "mTile" \
            -srcfolder build/dmg-staging \
            -ov -format UDZO \
            build/mTile.dmg

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: build/mTile.dmg
          generate_release_notes: true
